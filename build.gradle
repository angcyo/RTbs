if (getRootProject().hasProperty("allGradle")) {
    apply from: allGradle.lib_base
} else {
    apply from: '../lib/lib_base.gradle'
}

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    compileOnly project(":UIViewLess")
}